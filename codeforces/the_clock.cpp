#include <bits/stdc++.h>
#define lli long long int

using namespace std;

void read_arr() {
    string c;
    cin >> c;

    int every_minutes;
    cin >> every_minutes;

    int minute_shift_every_day = 1440 % every_minutes;

    

    cout << 0 << "\n";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int cases;
    cin >> cases;

    for (int i = 0; i < cases; i++) {
        read_arr();
    }
}